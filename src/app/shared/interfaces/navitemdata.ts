export interface NavItemData {
  sectionId: string; // Eindeutige ID zur Identifizierung der Sektion (z.B. 'Summary')
  title: string;      // Die angezeigte Bezeichnung (z.B. 'Summary')
  imagePath: string;  // Der Pfad zum Icon
}
